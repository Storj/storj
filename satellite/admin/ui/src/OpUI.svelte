<script lang="ts">
  // TODO: write a component that take the list of parameters and types and the
  // function to call (i.e. reference to a method of the API) and render a UI
  // (input texts fields with specify type, etc., and submit button)
  // In the future it will be good to read a Swagger operation API from JSON format
  // and render the UIR
</script>
